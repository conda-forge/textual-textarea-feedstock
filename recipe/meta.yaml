{% set name = "textual-textarea" %}
{% set version = "0.16.0" %}

package:
  name: {{ name|lower }}
  version: {{ version }}

source:
  url: https://pypi.org/packages/source/{{ name[0] }}/{{ name }}/textual_textarea-{{ version }}.tar.gz
  sha256: 494d8b956722500006e550a1c5122ebc916386027e3ce07c8b3dab07ccab4607

build:
  noarch: python
  script: {{ PYTHON }} -m pip install . -vv --no-deps --no-build-isolation
  number: 0

requirements:
  host:
    - python {{ python_min }}
    - poetry-core
    - pip
  run:
    - tree-sitter-python ==0.25.0
    - tree-sitter-sql ==0.3.7
    - tree_sitter ==0.25.2
    - python >={{ python_min }}
    - textual >=6.3.0,<7
    - pyperclip >=1.9.0,<2

test:
  imports:
    - textual_textarea
  commands:
    - pip check
  requires:
    - pip
    - python {{ python_min }}

about:
  home: https://github.com/tconbeer/textual-textarea
  summary: A text area (multi-line input) with syntax highlighting for Textual
  license: MIT
  license_file: LICENSE

extra:
  recipe-maintainers:
    - xhochy
